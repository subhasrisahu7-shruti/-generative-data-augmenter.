# -generative-data-augmenter.
A privacy-compliant tabular data scaling pipeline built in Google Colab using Conditional GANs (CTGAN) to upscale customer profile densities while fully preserving original dataset joint distributions and statistical correlation profiles.
